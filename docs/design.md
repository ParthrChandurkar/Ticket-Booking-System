# Design Notes

## Trade-offs & Known Limitations

Email delivery uses Resend's `onboarding@resend.dev` sandbox sender for the assignment demo, so confirmations are limited to the Resend account email until a custom sending domain is verified for production.
